lightgbm:
  num_leaves: 63
  learning_rate: 0.03
  n_estimators: 2000
  min_data_in_leaf: 200
  feature_fraction: 0.8
quantiles: [0.1, 0.5, 0.9]
cv:
  scheme: rolling_origin           # blocked by WHOLE DAYS (all 96 slots stay together)
  test_holdout_months: 2           # most recent months held out as final test
